@model PeopleDetailsVM

@{
    ViewBag.Title = "Details";
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title">@Model.Name</h4>
        <h6 class="card-subtitle mb-2 text-muted"></h6>
        <p class="card-text">
            @Model.Email
        </p>
    </div>
</div>

